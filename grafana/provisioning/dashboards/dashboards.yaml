apiVersion: 1

providers:
  - name: 'Default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # This is the path INSIDE the Docker container where Grafana will look
      path: /etc/grafana/provisioning/dashboards